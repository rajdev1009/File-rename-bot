# ─────────────────────────────────────────────────────────────────
# RAJ DEV — Telegram File Renamer Bot
# Developer : Raj Dev (@raj_dev_01)
# Pinned production dependencies — Python 3.10+
# ─────────────────────────────────────────────────────────────────

# Core Telegram MTProto client (bot + user account support)
telethon==1.36.0

# Async file I/O for safe temp file handling during concurrent downloads
aiofiles==23.2.1

# C-extension that accelerates Telethon AES encryption/decryption.
# Significant speed boost for large file transfers (optional but recommended).
cryptg==0.4.0

# Pure-Python AES fallback — used by Telethon when cryptg is unavailable
pyaes==1.6.1
